import 'package:flutter/material.dart';

class TriageChatScreen extends StatelessWidget {
  const TriageChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Triage')),
      body: const Center(child: Text('Triage Chat — coming soon')),
    );
  }
}
